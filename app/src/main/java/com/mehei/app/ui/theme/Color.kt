package com.mehei.app.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════
// MEHEI — Warm Indian Celebration Palette
// Inspired by henna art, marigold garlands, and terracotta pottery
// ═══════════════════════════════════════════════════════════════

// --- Primary: Deep Henna Maroon ---
val MeheiPrimary        = Color(0xFF8B2500)   // Deep henna maroon — primary actions
val MeheiPrimaryDark    = Color(0xFF5C1800)   // Darker maroon — pressed states
val MeheiPrimaryLight   = Color(0xFFF5D0C5)   // Soft blush — containers
val MeheiOnPrimary      = Color(0xFFFFFFFF)   // White text on primary

// --- Secondary: Terracotta ---
val MeheiSecondary      = Color(0xFFE2725B)   // Terracotta — highlights, accents
val MeheiSecondaryDark  = Color(0xFFA84B3A)   // Deep terracotta
val MeheiSecondaryLight = Color(0xFFFDE8E4)   // Light terracotta — containers
val MeheiOnSecondary    = Color(0xFFFFFFFF)   // White text on secondary

// --- Tertiary: Festive Gold ---
val MeheiTertiary       = Color(0xFFD4A017)   // Festive gold — accents, badges
val MeheiTertiaryDark   = Color(0xFF9A7410)   // Deep gold
val MeheiTertiaryLight  = Color(0xFFFFF3D0)   // Pale gold — containers
val MeheiOnTertiary     = Color(0xFF1C1917)   // Dark text on tertiary

// --- Surface / Background ---
val MeheiBackground     = Color(0xFFFFF8F0)   // Warm ivory — light mode bg
val MeheiSurface        = Color(0xFFFFFCF8)   // Cream white — cards
val MeheiSurfaceVariant = Color(0xFFF5EDE4)   // Warm tinted surface
val MeheiOnBackground   = Color(0xFF1C1917)   // Charcoal sand — text
val MeheiOnSurface      = Color(0xFF292524)   // Stone — body text
val MeheiOnSurfaceVar   = Color(0xFF78716C)   // Muted stone — captions
val MeheiOutline        = Color(0xFFD6D3D1)   // Stone border
val MeheiOutlineVariant = Color(0xFFE7E5E4)   // Subtle divider

// --- Dark Mode Surfaces ---
val MeheiDarkBackground = Color(0xFF0C0A09)   // Deep charcoal
val MeheiDarkSurface    = Color(0xFF1A1614)   // Warm dark surface for cards
val MeheiDarkSurfaceVar = Color(0xFF2A2420)   // Elevated warm dark surface
val MeheiDarkOnBg       = Color(0xFFFAFAF9)   // Light text
val MeheiDarkOnSurface  = Color(0xFFE7E5E4)   // Softer light text
val MeheiDarkOutline    = Color(0xFF3D3530)   // Warm dark border

// --- Semantic Colors ---
val MeheiError          = Color(0xFFDC2626)   // Error red
val MeheiOnError        = Color(0xFFFFFFFF)
val MeheiErrorContainer = Color(0xFFFEE2E2)   // Light red container
val MeheiSuccess        = Color(0xFF16A34A)   // Booking confirmed
val MeheiSuccessLight   = Color(0xFFDCFCE7)   // Success container
val MeheiFlashBadge     = Color(0xFFEAB308)   // Flash slot badge (yellow)
val MeheiHennaBrown     = Color(0xFF78350F)   // Henna art accent
val MeheiGoldShimmer    = Color(0xFFD4A017)   // Gold shimmer for premium elements
